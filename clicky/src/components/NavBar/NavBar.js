import React from "react";

const NavBar = () => (
    <nav className="navbar">
        <a href="/">Clicky Game</a>
        <h2 className="title">Click an image to begin!</h2>;
        <h2 className="scoreCounter">`Score: | Top Score: `</h2>
    </nav>
);
  
export default NavBar;